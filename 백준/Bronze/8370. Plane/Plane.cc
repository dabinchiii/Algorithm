#include <bits/stdc++.h>

using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int n1, n2, k1, k2;
    cin >> n1 >> k1 >> n2 >> k2;
    cout << k1 * n1 + k2 * n2;

    return 0;
}
